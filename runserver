cd ~/tf2server/steamcmd_linux/tf2
# TODO: allow -replay to be added somehow
# And if it's used, also start an HTTP server in the replays directory
# (using 'python3 -m http.server' or similar).
LD_LIBRARY_PATH=bin ./srcds_linux -console -game tf +sv_pure 1 +maxplayers 40 +map ${1:-cp_dustbowl} -nobreakpad
